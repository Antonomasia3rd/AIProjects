// Standalone translation unit for the experimental Live Tile background task DLL.
// Implementation lives in src/ga_livetile_background_task_dll.inc so the source stays consistent with the .inc-based main app layout.
// This wrapper must remain a separate .cpp because it builds a separate binary.
#include "../dependencies/DesktopStub/ga_livetile_background_task_dll.inc"
