// Standalone translation unit for the packaged WinRT Live Tile broker app.
// Implementation lives in src/ga_livetile_broker_app.inc so the source stays consistent with the .inc-based main app layout.
// This wrapper must remain a separate .cpp because it builds a separate binary.
#include "src/ga_livetile_broker_app.inc"
